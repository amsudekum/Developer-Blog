<div class="text-center">
  <h2>{{blog_name}}</h2>
  <p>{{blog_text}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span class="dollar-amount">{{format_amount needed_funding}}</span>
    needed
  </p>
  <p>
    <button class="btn btn-primary">Publish Your Blog</button>
  </p>
</div>
